/**
 * Человек: имя и возраст. Готовый data class, менять не нужно.
 */
data class Person(val name: String, val age: Int)

/**
 * Товар в корзине: название, цена и категория. Готовый data class, менять не нужно.
 */
data class Item(val name: String, val price: Int, val category: String)

/**
 * Задание: оставь только совершеннолетних — тех, кому 18 лет или больше.
 *
 * Порядок людей в результате — как во входном списке.
 *
 * Примеры:
 *  filterAdults(listOf(Person("Аня", 20), Person("Ваня", 15))) -> [Person("Аня", 20)]
 *  filterAdults(listOf(Person("Катя", 10))) -> []
 */
fun filterAdults(people: List<Person>): List<Person> = TODO("реализуй")

/**
 * Задание: посчитай суммарную стоимость всех товаров в корзине.
 *
 * Для пустой корзины верни 0.
 *
 * Примеры:
 *  totalPrice(listOf(Item("Хлеб", 60, "Еда"), Item("Молоко", 90, "Еда"))) -> 150
 *  totalPrice(emptyList()) -> 0
 */
fun totalPrice(cart: List<Item>): Int = TODO("реализуй")

/**
 * Задание: сгруппируй товары по категории.
 *
 * Ключ карты — название категории, значение — список товаров этой категории
 * в том порядке, в котором они шли во входном списке.
 * Для пустого списка верни пустую карту.
 *
 * Примеры:
 *  groupByCategory(listOf(Item("Хлеб", 60, "Еда"), Item("Тетрадь", 40, "Канцелярия")))
 *    -> {"Еда"=[Item("Хлеб", 60, "Еда")], "Канцелярия"=[Item("Тетрадь", 40, "Канцелярия")]}
 */
fun groupByCategory(items: List<Item>): Map<String, List<Item>> = TODO("реализуй")

/**
 * Задание: верни n наибольших чисел из списка, по убыванию.
 *
 * Если n больше размера списка, верни все числа по убыванию.
 * Для пустого списка верни пустой список.
 *
 * Примеры:
 *  topN(listOf(3, 9, 1, 7, 5), 3) -> [9, 7, 5]
 *  topN(listOf(2, 4, 4), 3) -> [4, 4, 2]
 */
fun topN(scores: List<Int>, n: Int): List<Int> = TODO("реализуй")
